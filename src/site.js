// Single source of truth for brand + links. Swap these when the project locks public URLs.
export const BRAND = 'Operator'
export const TAGLINE = 'Mission control for working agents.'
export const SUBLINE = 'You run the agents. Operator makes the work visible and steerable.'

// CTAs. DOWNLOAD_URL is a placeholder until signed releases are published.
export const DOWNLOAD_URL = '#download'
export const GITHUB_URL = 'https://github.com/juanmnl/operator'

// The accent: signal green — "live / running / healthy" in an ops dashboard. The one
// confident color over a near-black, slate-and-cream palette drawn from the logo.
export const ACCENT = '#2fe39a'
